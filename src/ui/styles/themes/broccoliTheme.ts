import { Theme } from './@types';
import { DefaultTheme } from 'styled-components';

export const broccoliTheme: DefaultTheme = {
    borderRadius: '5px',

    colors: {
      main: 'cyan',
      secondary: 'magenta',
    },
}